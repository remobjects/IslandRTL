'''<summary>The CDATASection interface represents a CDATA section that can be used within XML to include extended portions of unescaped text. The symbols &lt; and &amp; don’t need escaping as they normally do when inside a CDATA section.</summary>
<DynamicInterface(GetType(EcmaScriptObject))>
Public Interface [CDATASection]
End Interface